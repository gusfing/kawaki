{
  "name": "admin-dashboard-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI Agent-First Admin Dashboard for Kawaki",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:coverage": "pnpm -r run test:coverage",
    "lint": "pnpm -r run lint",
    "migrate": "cd backend && pnpm migrate",
    "seed": "cd backend && pnpm seed"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  }
}
